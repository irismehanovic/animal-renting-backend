server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/animal_renting_db
    username: postgres
    password: 2140
  jpa:
    hibernate:
      ddl-auto: update
    openInView: false
    show_sql: true
    generate-ddl: true
  data:
    jpa:
      repositories.enabled: true
